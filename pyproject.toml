[project]
name = "pdf2epub-glmflash"
version = "1.0.0"
description = "Scanned PDF to EPUB converter powered by GLM-5.3-Flash vision OCR"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = [
    "requests>=2.31.0",
    "pymupdf>=1.23.0",
    "EbookLib>=0.17.1",
    "python-dotenv>=1.0.0",
    "markdown>=3.5",
]
